[package]
name = "oncm-perf05-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "=3.0.6", default-features = false, features = ["std"] }
oncm-lean-checker = { path = "../checker", package = "oncm-perf05-checker" }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
