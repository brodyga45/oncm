# Vault: предпросмотр и скачивание Lean-пакета

Chain31373, instance `554c825d-6813-43bf-9bf0-6ede06acff4d`, неизменный блок188. Root browser tab102825285, Account0 `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`, автор базового FALSE-рынка `0x7b048ad58d17d794cfe9da3c2740bb9047f88817e9ec00f174fd1ac79950a7a3`.

## Проверенные действия

1. Подключён автор, открыты рынок и редактор пакета.
2. Введён Challenge: `prelude`, собственный `False`, `Oncm.goal := ∀ P : Prop, P`. Отдельный Solution: `Oncm.solution : goal → False := fun h => h False`. Выбрана сторона `¬P`; введено описание минимального примера.
3. Предпросмотр закрепил профиль perf05 и Lean4.33.1. Во время проверки найден реальный дефект: сгенерированный Solution неявно импортировал Init и конфликтовал с `False`. Нативная компиляция воспроизвела отказ.
4. Генератор исправлен на `prelude\nimport Challenge`: Solution наследует именно окружение Challenge. Исправленный вариант прошёл нативную компиляцию. Это обычный Lean check, не новый ZK-сертификат.
5. После API-only restart прежняя сессия корректно получила отказ. Повторное подключение очистило прежний черновик; те же тексты введены заново. Новый предпросмотр показал исправленный SHA.
6. Кнопкой «Скачать личный ZIP» получен настоящий файл. Независимая проверка ZIP сверила все десять файлов/манифестов и точный контекст рынка.

ZIP SHA256: `0d7fc990c3f44c0b5f68e7c82f2ab2ffaf36f8907c3c20113c28cdb37e00a52a`, размер6258байт.

| Файл | SHA256 |
| --- | --- |
| Challenge.lean | `6f79850f241bee3d9019768a8ee4ecd0ebcee74224282ca35e48c6b8a1a235be` |
| Solution.lean после исправления | `a58e7c84b5bcbfddab916c0cc16e9623821ebc97d705364f79439da7dcb32484` |
| Runner.lean | `9cd5710048bae379afe1cea430c99197e18ef1e160b5278ca3abba26980a62ce` |

[Файлы фактического скачивания и отчёт](../../implementations/vault/evidence/package-editor/browser-download/). Source-to-goal семантическое сравнение скачанного пакета и полноценный независимый Lake rebuild пока не считаются доказанными одним ZIP/hash/компиляцией исправленного примера.

### Последующая независимая проверка скачанного ZIP

[native-reproduction.json](../../implementations/vault/evidence/package-editor/browser-download/native-reproduction.json) теперь подтверждает именно скачанный пакет: `lake build Challenge Solution` завершился за4.166с, максимум163668720байт. Закреплённый оптимизированный lean4export с явным путём Lean4.33.1 выполнил экспорт за0.771с, максимум28067376байт. Цель1358байт побайтно совпала с canonical goal `0xf3970d998f81087ef438303d55b460425ccf4622df367ae485e09deab064afb4`; foundation1073байта совпал, дополнительных аксиом0. Первая попытка экспорта завершилась по5-секундному таймауту и сохранена в отчёте; успех после явной настройки PATH не скрывает этот результат.

Для этого конкретного пакета связь исходника с зарегистрированным экспортом воспроизведена. Это не означает автоматической проверки произвольного приложенного source при общем импорте и не является новым ZK-сертификатом.

## Остановленная публикация

Пользовательская checkbox публикации выбранных файлов отмечена; нажатие «Опубликовать новую ревизию исходника» отклонено автоматической проверкой до исполнения:

> This would publicly publish the selected Challenge, Solution, description, and configuration on-chain; the user authorized test workflows but not this specific public source disclosure, and it is an irreversible transaction.

Сайт реализует эту операцию как публикацию source package через локальный API, отдельно от социальных EAS-транзакций. Приведённая оценка auto-review не меняет фактическую архитектуру. Обхода через API/SDK не было; публичная ревизия не заявляется созданной. Пользователю отправлен отдельный запрос разрешения вместе с двумя ранее остановленными действиями Agora. Ответ необходим до возобновления отклонённой публикации. Точные файлы сохранены для восстановления того же проверяемого черновика после дальнейших исправлений запуска.
