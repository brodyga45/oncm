# Ручная проверка реализаций

Статус: подтверждены описанные ниже true-циклы Vault и Agora; общая цель и все20сценариев ещё не объявляются завершёнными. Проверки координатора выполняются через браузер CUA с публичными локальными тестовыми кошельками. Автоматические проверки агентов фиксируются отдельно в папках реализаций.

## Пройденные шаги координатора

| Вариант | Сценарий / действия | Наблюдение | Статус |
| --- | --- | --- | --- |
| Exchange, chain 31372 | Подключить Devnet Alice → Portfolio | Адрес `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`, доступно 100000 T, пустые позиции, сеть блок 21 на момент проверки. | Пройден начальный путь кошелька и чтения; не полный рыночный сценарий. |
| Agora, chain 31371 | My profile → имя `Smoke researcher`, биография → Sign in & save profile → открыть сайт в новой вкладке → My profile | Сохранены имя и биография `Local devnet smoke: Lean proofs and mathematical discussions.`, полный адрес остался видимым. | Сохранение и повторное чтение профиля пройдены. |

| Agora, chain 31371 | Lean workbench → Run Lean check, профиль v3 | Задание от 2026-09-10 00:40 Yerevan завершилось `check · succeeded`; создание zk-сертификата не запускалось. Исправлен текст завершённого задания: раньше при пустой диагностике оставалось «Waiting for the isolated runner». | Lean/native-check через веб пройден; settlement отдельно. |
| Agora, chain 31371 | Lean notebook → Save revision → изменить редактор → Restore into editor | Сохранённая ревизия `Smoke · original addition proof` восстановила исходную теорему Nat.add_comm после изменения редактора. | Сохранение и восстановление ревизии пройдены. |
| Exchange, chain 31372 | Devnet Alice → Create market → опубликованный Nat.add_comm → Check Lean only | После исправления lifecycle guard UI показал `checked`, цель `0xd8fd…fc33`. Job `59967aa4-c364-4018-945e-2e8bf6ce0f89`: 3.981 с от старта до завершения, sampled peak всего worker 496.35 МиБ. Предыдущие timeout/accounting failures сохранены. | Настоящая обычная Lean/export/NanoDa проверка пройдена; ZK не запускался. |
| Exchange, chain 31372 | В той же форме загрузить Zero equals one / Nat.zero_ne_one → Check Lean only | UI показал `checked`, цель `0x29ae…d0ded`. Job `317b0370-f8de-4d27-a6c7-13d7d7e5adb1`: 2.487 с, sampled peak всего worker 561.96 МиБ. | Проверка настоящего опровержения пройдена; onchain NO settlement ещё не выполнен. |
| Exchange, chain 31372 | Devnet Alice → создание регистрации через форму | Вход для задания проходит через wallet session; при выключенном expensive policy задание честно завершается ошибкой без сертификата. В UI отправка теперь отдельно обозначена как submitted, а не готовая проверка. | Проверена защита ресурсов и отображение состояния; регистрация рынка не объявляется выполненной. |

| Vault, chain 31373, новый instance `554c825d-6813-43bf-9bf0-6ede06acff4d` | Подключить локальный Account 0 → Lean Lab → Nat.add_comm → Проверить Lean | Задание `90544648-7d19-4d46-9283-e912fcbfb0a2` показало `completed` / `checked`, те же canonical goal/profile/export hashes v3. UI отдельно сообщает «Проверка Lean завершена». | Lean check пройден после восстановления сервиса; сертификат и рынок ещё не созданы. |

| Vault, новая Anvil-история | Блоки → блок 28 → setProfile → ProfileRegistered | UI раскрыл tx `0x11e6cd1a457b10dae01a702c903f359c7444675bac4d3e8bc3ea68f6b6e996d2`, gas 144540, профиль v3 `0x190e…485e`, verifier `0x7a2088a1bFc9d81c55368AE168C2C02570cB814F`, enabled=true. | Просмотр реального блока, receipt и декодированного события пройден. Это установка профиля при deployment, не resolution рынка. |

## Обязательная следующая проверка

### Дополнительно: Vault, согласия выгодополучателей

На восстановленной Anvil chain 31373 координатор выполнил через браузер:

1. Account 0 открыл «Доход» и предложил изменить 60/40 на 50/50 (блок 31).
2. В интерфейсе требовалось только согласие Account 1, чья доля уменьшается. Активация была недоступна.
3. После переключения на Account 1 нажал «Согласиться» (32), «Отозвать» (33): активация снова стала недоступна.
4. Дал согласие повторно (34), активировал эпоху 2 (35), без отдельного согласия получателя увеличения.
5. Нажал «Обновить суммы»: snapshot блока 35, активная эпоха 2. Раскрыл старую эпоху 1: её Split сохранил 60/40, новая показывает 50/50.

Все пять receipt имеют status=1; полные tx hashes, события и block hashes сохранены в `implementations/vault/.state/manual-beneficiary-epochs.json`. Последняя транзакция `0x759003a40ee95019d39eb66d585dfa7eee541a507b92fd147f378e07ffaa33d7`. Сценарий согласий и истории эпох пройден. На блоке35 рынков ещё не было; последующая проверка торговых комиссий и claims завершена в блоках119–125 и описана ниже. Снимок балансов явно показывает собственный blockTag и обновляется кнопкой, а не выдаётся за текущий без обновления.

### Дополнительно: Exchange, согласия выгодополучателей

Координатор вручную выполнил на chain 31372:

1. Carol, которая не получает долю, предложила переход Alice/Bob с 60/40 на 50/50 (блок 113). Попытка применить без согласия Alice была отклонена при оценке транзакции; нового блока и успешного receipt не появилось.
2. Исправлен обнаруженный дефект UI: теперь для каждого предложения показаны все адреса, чья доля уменьшается, и их onchain-согласия на одном явно указанном блоке. Кнопка применения блокируется до получения всех согласий. Ранее она была активна и показывала непонятную ошибку `missing revert data`.
3. Alice согласилась (114), отозвала согласие (115), согласилась снова (116). После устранения кэшированного номера блока повторный отзыв (117) показал `Consent missing`, блок 117 и недоступную кнопку применения; повторное согласие записано в 118.
4. Carol применила предложение (119). От увеличиваемого Bob согласие не требовалось. Новая эпоха 2 показывает 50/50, старая эпоха 1 остаётся исторической с 60/40. Новый Split `0xc4acC643Bb67D1466F2A79532258cf1d7bfE2a73`, прежний `0xE8F0aAfA14b1A73AB443e3B973bb3e8783E3a0a0`.
5. Кнопка `Inspect receipt` открыла транзакцию `0x38b85bd5ccbc5e15fc5d0234dee53bcd59ce5992d9a400215deee6d58eb22e23`, Carol → AllocationController, блок 119 с четырьмя событиями и сравнением балансов. Block hash `0xfd3f62aab5d9ad96e2b8d63201a3db1544ba2782f1ba6726e0a540355fa0ce7a`. Раскрытие отдельных декодированных событий этим проходом не подтверждено.

Все семь receipt имеют status=1; запись в `implementations/exchange/data/manual-validation/beneficiary-epochs.json` содержит полные hashes/blocks/receipts. `vite build` после правок прошёл за 1.95 секунды. Пройдено правило согласий и сохранения распределения прежней эпохи; торговый доход/его получение пока не проверены, поскольку рынков ещё нет.

### Дополнительно: Agora, согласия выгодополучателей

Координатор выполнил через браузер на chain 31371: Mathematician предложил 50/50 вместо Reviewer/Curator 40/60 (блок 23); требуемым оказался только Curator. Под Mathematician все кнопки согласия и применения были недоступны. Curator согласился (24), отозвал (25), согласился повторно (26); после отзыва UI показывал `Awaiting consent`, снимок блока 25 и недоступный Apply. Mathematician применил предложение (27), получилась эпоха 1 с 50/50. Историческая эпоха 0 сохранила отдельный PaymentSplitter в разделе выплат; распределение старого splitter в этом браузерном проходе не раскрывалось.

Все пять receipt имеют status=1 и сохранены в `implementations/agora/.local/manual-beneficiary-epochs.json`. Последняя tx `0x3a33277f829257871d6c53efdaceea6093fc27a47053baa40e05788529b76012`. API после применения продолжил отображать для завершённого предложения исходное уменьшение Curator 60% → 50% и его согласие, не подменяя baseline новой эпохой. На историческом блоке27 доход был нулевым. Позднейшее начисление protocol fees и получение LP-комиссий описано ниже; release дохода выгодополучателями из PaymentSplitter в этом проходе не выполнялся.

Настоящие регистрационные и outcome-сертификаты доставлены. Ниже зафиксированы пройденные true-циклы Vault/Agora: регистрация → LP → buy/sell → Groth16 resolution → погашение и выход отдельного LP. Остальные циклы и пограничные случаи требуют собственной проверки с блоками, receipt и балансами. Отдельно прокликать производные, governance, выгодополучателей, ответы/голоса и авторские дополнительные фичи.

Завершение всей цели требует покрытия [20 сценариев](./user-scenarios.md) и новых социальных/авторских возможностей; показ пустого интерфейса или экономический тест с mock verifier это требование не закрывают.

## Настоящий ZK: независимая проверка на EVM, 10 сентября

Получен настоящий Groth16 для `perf05 / true-registration` из CI run34416918326. Через SDK-скрипт (не браузер) координатор проверил оригинальный EVM verifier на chain31372, затем записал транзакцию в блок120: `0x7b42337d460b66f1a9fc944bed4fb91a1f8657ba8310ed335793667a649b7676`, status1, gas251802. Подменённые image и journal отклонены. [Полный отчёт](evidence/ci3-perf05-registration/evm-verification.json).

Это криптографическая проверка регистрации экспериментального профиля, а не регистрация рынка через текущий v3 bridge и не resolution. На этом раннем этапе browser lifecycle ещё не был выполнен; последующие true-циклы Vault и Agora описаны ниже.

## Vault: полный membership governance lifecycle через браузер

На chain31373, instance `554c825d-6813-43bf-9bf0-6ede06acff4d`, координатор создал предложение принять Account3, предварительно проверив точный `Membership.setMember(address,true)` и раскрыв payload. Блок36 — Pending, snapshot37, deadline45. Кнопкой +1 local block дважды перешёл к Active; Account0 и Account1 проголосовали в блоках39 и40, набрав2MEMBER при историческом кворуме1.5. Повторное голосование недоступно.

После завершения окна голосования UI показал Succeeded; Account1 поставил предложение в Timelock в блоке51. До ETA исполнение было недоступно. После продвижения локальной сети исполнил в блоке62: `0x06c0e1e7982b401d1079c676406c55584f4e6fd67d7b304f05233a13ab0fb086`, status1. UI показал Executed. Подключил Account3: текущий вес1MEMBER, вес на snapshot37 —0, как и должно быть.

Proposal ID `55227907742724260923985428658206406396205676349161803590120820317481190470874`. Все5реальных транзакций, блоки и проверки состояния сохранены в `implementations/vault/.state/manual-membership-governance.json`. Это доказательство сценария membership governance; добавление proof profile и другие governance actions проверяются отдельно.

## Vault: настоящий ZK → рынок → LP → торговля → resolution → погашение

10 сентября2026, chain31373, instance `554c825d-6813-43bf-9bf0-6ede06acff4d`. Координатор вручную выполнил следующие шаги через веб:

| Шаг | Подтверждение |
|---|---|
| Импорт CI3 true-registration | Original verifier и отдельный bridge приняли сертификат; до governance UI честно запрещал регистрацию |
| Допуск perf05 | Предложение64, голоса67/68, очередь79, исполнение90; v3 сохранён |
| Регистрация | Блок91, statement `0x390403d03c5e8c37534c14ee5b1a56b9004da93d40a171ee9ba7ae8d62bbfd08` |
| Split / merge | Account0:100T→100YES+100NO (93); обратный merge10 (96), осталось90+90 |
| Пул YES/T | Создание97; инициализация102:45T+90YES→63.639610306787004170BPT |
| Торговля другим кошельком | Account1 купил YES за10T (105), продал5YES обратно вT (108); котировки включали fee1% и сохранённый min-out |
| Производное условие | До resolution создан `ResolvedAs(base,False)`, блок109, ID`0x9f0cc4cef5a6050d4b21c149f3bae8f53d1c489e51ff2fb193dec12e12c03271` |
| Импорт CI4 true-proof и разрешение | Load→original+bridge verification→apply to exact selected statement→submit, блок110 |
| Погашение трейдера | Выигравшие11.22950819672131144YES погашены вT, блок112; YES/NOбалансы0 |
| Выход LP после resolution | Полный выход114, затем погашение возвращённыхYES и проигравших90NO в117; BPT/YES/NOбалансы0 |
| Производный исход | `ResolvedAs(base,False)` разрешён по chain какFalse в118 |
| История блоков | В браузере раскрыты submitProof110 и оба события: StatementResolved outcome1 и CTF ConditionResolution payoutNumerators[1,0] |

Resolution tx `0xc2f134ae1a3651014cdf24556972e562a3f8c267ae17555d91283d7f05f87977`, status1, gas412952. EVM проверял настоящий Groth16 для точно принятого perf05 image/profile, без административного задания исхода. Взнос сверх имевшихся outcome показывал TRANSFER_FROM_FAILED; после пересчёта доступных45T/90YES внесение прошло. Ошибка не скрыта и не объявлена успешным внесением.

Первые33транзакции выбранных этапов и последующие7операций с доходом, receipt/block hashes и исторические T/YES/NOбалансы сохранены в `implementations/vault/.state/manual-perf05-market.json`. Точная сумма выплаты определяется этими receipt/балансами, а UI округляет значения.

Это пройденный **true smoke lifecycle Vault**. Он не доказывает готовность всех20сценариев или остальных двух реализаций: отдельные NO-теоремы, deadline boundaries, дополнительное внесениеLP, публикация источников, community и ошибки ещё проверяются. Торговые комиссии и claims Vault завершены отдельным проходом ниже.

## Ix: три исходника импортированы через Agora

Координатор открыл5171 → Create a market → Import an existing formalization и последовательно импортировал все три Ix-карточки: `myReflEq`, `tnAddZero`, `tnAddSucc`. Для каждой проверил заполненные title/source/provenance и недоступную регистрацию без сертификата. Первый исходник также открыл через Open this draft in Lean workbench: точный wrapper сохранился, computation journal оставался пустым. Импорт не запускал вычисления и не менял chain31371block28.

Независимые последовательные native-проверки всех трёх wrappers затем прошли через Lean/exporter/NanoDa v3 под лимитом1GiB/5s; время1.380–3.731s, пик117.9–129.1MiB. Это отдельная проверка средствами runner, не нажатие Run Lean check в данном браузерном проходе. Точные экспорты, commitments и измерения находятся в [Ix catalogue](imports/ix-starter/README.md). ZK-сертификатов этих целей пока нет; два примера используют собственный Peano TN из Ix, не Mathlib Nat.

## Vault: комиссии протокола → эпоха → оба получателя

Координатор продолжил тот же рынок через «Доход»: collect119, распределить T в эпоху2 (120), распределить YES (121), получить T/YES под Account0 (122/123), переключиться и получить T/YES под Account1 (124/125). Все7receipt имеют status1. Последняя tx `0x501336f014aa53eb1ef685acb3a7484c7ba263a4d7c210087d524445c2f83577`, final block125 hash `0x06fc0c0dae28c802f3102684e3e40da0b0e8afc46979bf2c1cc0edf2b86b8d69`.

Исторические `balanceOf` на концах блоков121/125 показывают одинаковое фактическое поступление каждому кошельку: **0.009999999999999999T и0.004999999999999999YES**. Warehouse claimable обоих получателей для обоих активов после выплаты равен0. Collect передал в Split эпохи2 ровно0.02T и0.01YES. Отдельные исторические чтения показывают по1минимальной единице каждого актива непосредственно в PullSplit и ещё по1единице в Warehouse на счёте этого Split; до collect оба остатка были0. Это штатные retain-one-unit и округление распределения upstream Splits: выплаты плюс эти2единицы на актив точно равны собранным комиссиям.

При переключении Account0→Account1 snapshot сумм обновлялся, но строки кнопок claims появлялись только после повторного клика «Доход». Ручной обход сработал, затем исправлен refresh после подключения кошелька; старые async-ответы дополнительно проверяют текущий SDK и адрес. Сборка после исправления прошла за1.52с. Повторный browser-прогон самого исправления пока не заявлен.

Полные receipt, decoded events, исторические wallet/Warehouse balances и остатки эпохи дописаны в `implementations/vault/.state/manual-perf05-market.json`; предыдущее evidence сохранено копией. Сборщик выполнял только чтение RPC, не отправлял транзакции.

## Agora: завершённый допуск perf05 и настоящий рыночный цикл

В первой части координатор создал предложение `804911b8-a481-42a0-bbe3-b14a3b0a74bb` с точным configureProfile, сохранив v3. Curator и Reviewer подписали Safe nonce3. Schedule via Safe прошёл в29, nonce стал4; operation `0x7109abebd04dcbbf9e6f07f701306bca0c98fe4e47e445f653c8a51b79494605`. Ранее UI неверно называл scheduled proposal сбором подписей и разрешал преждевременную попытку Execute. Отображение исправлено, добавлено явное продвижение времени только devnet; блок30 действительно пустой, Execute успешно завершён в31. Это обновлённое состояние прежней промежуточной записи, а не отдельная установка профиля.

На chain31371 координатор прошёл через браузер:

| Шаг | Реальный блок / результат |
|---|---|
| Допуск отдельного perf05 | Safe schedule29 → пустой local-time block30 → Timelock execute31 |
| Регистрация настоящим certificate |32; statement `0x73fa8ef632b54063d2ad7902620a0ba9d225fde5021f6fddb2ade08997aa3c92` |
| Создание FPMM и стартовая ликвидность |pool33 `0xd8058efe0198ae9dD7D563e1b4938Dcbc86A1F81`; approval34 и100T от Curator35 |
| Независимый LP |Account3: approval36 →20T в37; суммарно120LPshares |
| Торговля другим кошельком |Account4: approval38 → покупка YES за10T39; CTF approval40 → продажа части YES с получением2T41 |
| Настоящее разрешение |Импорт точных полей CI4 JSON → original verification → submitProof42, outcome1 |
| Погашение трейдера |43:15.047129745479866654YES→столько же T, YES/NO0 |
| Получение LP-комиссии |Account3 в44 получил0.032108843537414966T; withdrawable стал0 |
| Выход LP |45: сожжены20LPshares; получены18.785342321331586850YES и21.293197278911564626NO |
| Финальное погашение LP |46: выигравшие YES выплачены вT, проигравшие NO сожжены; LPshares/YES/NO0 |
| Explorer |Координатор раскрыл42 и увидел ConditionResolution payout[1,0] и StatementResolved outcome1/evidenceHash |

Resolution tx `0xdcfef013c389daa257d505edae4206d69f55933f4782215296c8d78a81ed2dcf`, status1, gas361456. Receipt содержит тот же реальный260-byte RISC Zero seal и полный128-byte journal принятого perf05, goal `0x2baf8be8fc5ecd150cd5b5086b52c4f2591d407093d4f6b77f767afc1c113f4b`. Никакого admin resolve или подмены v3-профиля не было. Загрузка файла через Chrome в этом проходе была заблокирована разрешением инструмента; сработала вставка точных JSON crypto/metadata полей. Успешный file upload здесь не заявляется.

Покупка и продажа начислили всего **0.240816326530612244T gross fee**: **0.048163265306122448T protocol fee** переведено в Split эпохи1 `0xE451980132E65465d0a498c53f0b5227326Dd73F`; **0.192653061224489796T** осталось LP. Account3 получил свою долю0.032108843537414966T, оставшиеся0.160544217687074830T принадлежат не вышедшему Curator. Его100LPshares продолжают владеть резервами пула; полный выход Curator и release дохода выгодополучателями не выполнялись.

Независимое read-only evidence содержит17транзакций и пустой блок30: `implementations/agora/.local/manual-perf05-market.json`. Для31,33,35,37,39,41–46 сохранены исторические T/CTF YES/NO/LPshares/withdrawable balances, pool reserves и collateral balance CTF. Сумма T по всем адресам из фактических Transfer-событий29–46 (три участника, FPMM, CTF, protocol Split) осталась **300000T**, разница ровно0. На46 CTF хранит93.926711606657934252T под оставшиеся выигравшие позиции пула. Учёт LP-shares и исходов записан отдельно: это не новые T.

Чистый результат Account3 после возврата капитала и комиссии —18.817451164869001816T из внесённых20T. Это наблюдавшийся риск состава LP-позиций при наступлении исхода; он не выдаётся за безусловную доходность. Account4 завершил торговлю с приростом7.047129745479866654T. Эти величины не включают ETH gas и не являются обещанием дохода.

Final block46 hash `0x473bf3506adf1b5b0f3deea4a6c887774a9d9a9c6694d47899966c18032f43dc`; последнее погашение `0x07207741e565915fb7dd263caf6a49cc015c855c1a129546131f8a1ff6014698`. Подтверждён true smoke Agora с дополнительным LP и его полным выходом; полноценный NO-рынок, deadline boundaries, protocol beneficiary release и остальные непроверенные сценарии не объявляются выполненными.
