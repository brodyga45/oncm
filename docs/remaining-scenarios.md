# Что ещё требуется для завершения трёх реализаций

**Изменение объёма по поручению пользователя 2026-09-10:** активна колонка **Vault** и новые US-023 работы. Agora/Exchange заморожены; их незавершённые ветки сохранены как история, а не задачи команды. [Основной план Vault](vault-focus.md). Таблица не заявляет завершённость всех трёх.

## Актуальная матрица — 10 сентября 2026

**Это текущий срез; исторический аудит ниже свёрнут и не является действующим списком задач.** Основание: [23 пользовательских сценария](user-scenarios.md), [ручной журнал](manual-validation.md), текущие исходники и более поздние app/RPC evidence. Ни одна реализация пока не объявлена полностью принятой по всем веткам. Блоки относятся к разным независимым цепям: Agora31371, Exchange31372, Vault31373.

Текущий обязательный путь: **внешне подготовленный настоящий сертификат → точная проверка оригинальным EVM verifier и соответствующим bridge → регистрация/резолв кошельком**. Generic external bundle работает во всех трёх, без allowlist двух целей; все три прошли обычный браузерный импорт настоящего известного perf05 сертификата. Agora и Vault дополнительно прошли подмену goal → сброс готовности → читаемый отказ → восстановление исходного пакета без транзакций. Source hash означает целостность исходника, а не соответствие его смысла goal export. Два минимальных Lean-утверждения и четыре настоящих CI-сертификата уже дают принятый пользователем лёгкий smoke; новая arbitrary-Mathlib/FLT теорема и новый тяжёлый proving **не добавляются как условие завершения**. Более широкая совместимость остаётся явно ограниченной и документируемой.

Полные тексты профилей, комментариев, ответов, голосов/истории и блогов теперь **ончейн во всех трёх**. Прежние офчейн-проходы не подменяют новую приёмку. Генерация или заказ сертификата из сайта US017 **отложены пользователем**; обязательный remote dispatch больше не числится пробелом текущей версии. CLI, история и optional native check могут сохраняться. Ни mock verifier, ни успешная Lean-компиляция не называются ZK-доказательством.

### Самый поздний подтверждённый проход

Exchange deadline matrix завершена через228: подтверждённый parent225 без повторной отправки, timely226 True, opposite227 False, matching228 True; expired222 остаётся False. [Полные receipts и assertions](../implementations/exchange/docs/evidence/derived/README.md), [ручные действия](evidence/exchange-deadlines-browser-2026-09-10.md). Лимит браузерного инструмента перестал блокировать работу при повторной проверке доступности. Новый pending/readiness UI читает actual Solidity outcome в одном блоке;5 SDK tests/build прошли, resolved-state panel проверен в браузере; новый actual pending child230 также прошёл Refresh/disabled Resolve безTX. Exchange арбитраж и журнал теперь подтверждены до242, Agora operator до93, двухадресное согласие до97.

Новые остановки Vault: после обычной регистрации parent189 и future child190 automatic approval review отдельно отклонил создание past2020 condition и разрешение parent189. Пользователю отправлены конкретные дополнительные запросы; до ответа не повторять и не менять chain/API ради обхода. Старые Agora NO/matching и Vault source publication остановки остаются отдельными.

### Дополнительные подтверждённые результаты

- [Agora generic browser90](../implementations/agora/evidence/generic-browser/README.md): обычный paste/Verify, exact goal/source hashes, original EVM и bridge; altered goal отказ и восстановление. Ошибка API исправлена установкой Fastify error handler до регистрации plugin routes. Координатор повторно проверил16 связанных тестов, все прошли.
- [Vault generic browser190](../implementations/vault/evidence/generic-external/browser/README.md): anonymous reader, actual original EVM + admitted perf05 bridge; подмена goal очищает Apply и выдаёт `Goal bytes differ from claim`; восстановленный пакет проходит повторно. Блок190 не изменился.
- [Agora package reimport](../implementations/agora/evidence/package-reimport/README.md): actual downloaded JSON восстановлен обычным paste UI; changed file hash refused, certificate отдельно проверяется; wallet switch clears draft. Блок82 не изменился.
- [Vault fresh-start](../implementations/vault/evidence/fresh-start/README.md): runtime и собственный browser pass сохранены вместе с отдельным QA acknowledgment timeout. Прежняя временная остановка браузера по usage limit закончилась; это не снимает отдельных отказов approval review на конкретные записи.

### Как читать доказательства

**Веб** — действие действительно выполнено через интерфейс; денежные результаты подкреплены receipt/балансами. **К/Т** — конкретный путь в коде и/или тестах, без заявления ручного прохождения. **Частично** — основной путь прошёл, указан остаток. **Ожидает** — действие не подтверждено. Успешная строка основного пути не закрывает все ошибки, границы и восстановление.

- **[A-TRUE](manual-validation.md#agora-завершённый-допуск-perf05-и-настоящий-рыночный-цикл)**: governance29–31, TRUE market32–46. **[A-NO](../implementations/agora/evidence/no-cycle/README.md)**: регистрация/LP/NO trade47–54, settlement остановлен. **[A-доход/derived](../implementations/agora/evidence/claims-derived/README.md)**: обе выплаты74/75, opposite-outcome derivative76–78. **[A-LP](../implementations/agora/evidence/lp-recovery/README.md)**: отдельный split/merge79–81, частичный выход82; [текущий app audit](../implementations/agora/COVERAGE-AUDIT.md).
- **[B-TRUE/доход](evidence/exchange-true-fees-browser-2026-09-10.md)**: TRUE158–197, collect/distribute/обе LP-выплаты198–201. **[B-generic-NO](evidence/exchange-false-generic-browser-2026-09-10.md)**: общий bundle202, LP/trade, настоящий FALSE212, redemption/LP exit до218.
- **[C-TRUE](manual-validation.md)**:91–118; [доход119–125](manual-validation.md). **[C-NO/derived/LP](evidence/vault-false-browser-2026-09-10.md)**: FALSE148–169, pending/вложенная зависимость/дополнительный взнос/частичный выход170–188.
- **Social:** [A](../implementations/agora/evidence/onchain-social/README.md), [B](../implementations/exchange/ONCHAIN-SOCIAL.md), [C](evidence/vault-onchain-social-browser-2026-09-10.md). Это новые SSTORE2/ECP/EAS записи с полным содержимым, не старые SIWE-only публикации.
- **Packages/generic:** [A](../implementations/agora/GENERIC-CERTIFICATE-IMPORT.md), [A download/extras](../implementations/agora/evidence/final-scope-audit/README.md), [B](../implementations/exchange/docs/PACKAGE-START-AUDIT.md), [C downloaded ZIP + native reproduction](evidence/vault-package-browser-2026-09-10.md), [общий формат](external-certificate-compatibility.md). Реальные [CI3–CI6](ci-proof-validation.md) — отдельный криптографический evidence.

**Новое требование пользователя:** [governance как выгодополучатель](governance-beneficiary.md), US022 — входит в активную цель всех трёх реализаций. Treasury UI/SDK есть во всех трёх. Agora прошла доход/consent до110, Vault до231; Exchange проходит собственный цикл. Внутренняя выплата пока не считается выполненной. [Независимая проверка Agora координатором](evidence/agora-treasury-root-review-2026-09-10.md), [16 Vault receipts и инварианты](../implementations/vault/evidence/treasury/dao-cycle-rpc.json).

### Все23 сценария

| US / сценарий | Agora | Exchange | Vault | Конкретный остаток |
| --- | --- | --- | --- | --- |
|001 Создать математический рынок|**Веб:** A-TRUE и A-NO; generic browser90 + binding refusal|**Веб:** B-TRUE и общий bundle B-generic-NO202|**Веб:** C-TRUE и C-NO148; generic browser190 + binding refusal|Generic browser gap закрыт во всех трёх; новый рынок из каждой проверки повторно не создавался. У A восстановление register→pool→fund реализовано/проверено тестами, принудительный прерванный browser flow ещё не пройден. Не требуется новый prover.|
|002 Внести LP|**Веб:** второй LP37; точное imbalanced preview81|**Веб:** два LP177/182; NO207|**Веб:** init154/177, дополнительный join179 с query|Ручные отказы balance/allowance и изменившийся preview; multi-pool UX ограничен в A, SDK принимает pool явно.|
|003 Торговать|**Веб:** YES39/41 и NO52/54|**Веб:** YES184/186, NO209/211|**Веб:** YES105/108, NO156/159|Expired/stale quote, min-out/max-in и wallet refusal в вебе; контрактные отрицательные тесты не заменяют читаемую UI-ошибку.|
|004 Доказать P|**Веб:** genuine TRUE42|**Веб:** genuine TRUE187|**Веб:** genuine TRUE110|Wrong-goal/profile/outcome и повторная отправка: реальные read-only crypto/тесты есть, отдельные browser failure cases ограничены.|
|005 Опровергнуть P|**Частично:** CI6 проверен; отправка остановлена auto-review|**Веб:** generic genuine FALSE212, CTF[0,1]|**Веб:** genuine FALSE161, CTF[0,1]|Только A: прежний NO settlement и последующие выплаты ожидают решения пользователя; отказ не обходить.|
|006 Резолв к сроку|**Веб:** pending84/noTX, expired False87, timely True90; [A evidence](../implementations/agora/evidence/deadlines/README.md)|**Веб:** pending220/noTX; expired222, parent225, timely226, opposite227, matching228; expired сохраняется после late parent|**Частично:** parent189/future190 созданы; pending190 безTX; expired creation и parent resolution остановлены auto-review|Vault продолжение после конкретного разрешения пользователя. Точное равенство timestamps отдельно покрывается контрактными тестами; не заявлять browser equality. A late-base и kind3 ветки отдельно от kind1 pass.|
|007 Определённая сторона|**Веб:** opposite predicate FALSE78; matching draft остановлен|**Веб:** parent ResolvedAs(True)225; ResolvedAsBy(False)→False227; ResolvedAsBy(True)→True228|**Веб:** matching True162, opposite False182, вложенный True183; понятный pending171/190|A matching после разрешения; A/C deadline+outcome combinations ещё не подтверждены браузером. Custom operator registration — веткаUS009.|
|008 Погасить позиции|**Веб:** TRUE43/46|**Веб:** TRUE189/193/197; NO214/218|**Веб:** TRUE112/117; NO164/169; nested188|A NO после settlement; ясный repeat/zero/loser-only preview и browser refusal. У B остаются проигравшие YES с нулевой выплатой, это не выигранный T.|
|009 Расширить протокол|**Веб:** Safe/Timelock допуск perf05; admitted ResolvedAfter→kind4→False91–93|**Веб:** Governor/Timelock perf05123–143; [UnresolvedBy admission243–263 и condition264→False265](../implementations/exchange/docs/evidence/operator-browser/README.md)|**Веб:** membership36–62 и perf05 governance64–90|A: [actual operator use](../implementations/agora/evidence/operator-governance/README.md) после существующего admission12/14 подтверждён; B actual operator admission/use265 подтверждён; C остаётся. Disable/stale/неправомочный proposal — отдельные ветки. Raw ABI/API не равны универсальным типизированным формам новых операций.|
|010 Перераспределить доход|**Веб:** propose/consent/revoke/apply23–27; оба уменьшающихся consent94–97; три consent99–101→apply102|**Веб:**113–119; новый DAO-проход записывается отдельно|**Веб:** два consent191–193; новое предложение DAO20 с двумя согласиями194–197; DAO-consent230→apply231|C старое proposal1 стало stale после применения отдельного proposal2, его payload не менялся. A три человеческих veto и последующий отдельный DAO veto подтверждены. B новый multi-loser evidence ожидается; удаление получателя — отдельная ветка.|
|011 Получить комиссию|**Веб:** оба beneficiary74/75, exact T|**Веб:** protocol LP collect198→Warehouse→оба получателя200/201|**Веб:** creator fees119, distribute120/121, оба получателя122–125|Ненулевая старая эпоха. B получил LP, а не T; дальнейший LP→активы/погашение вручную ещё не подтверждён. LP-комиссия A44/82 не подменяет protocol income.|
|012 Внешние формализации|**Веб:** три Ix draft; native v3 всех3 отдельно подтверждён|**Веб:** [Palomar exact snapshot→draft](../implementations/exchange/docs/evidence/palomar-browser/README.md),7 files, no certificate readiness|**Веб190:** [Ix exact snapshot + Palomar v2 import](../implementations/vault/evidence/snapshot-import/README.md); mismatch Lean4.34.0-rc1/4.33.1 показан; old certificate cleared|A/B/C browser import подтверждён в указанном объёме. Интерфейс выбора declarations и автоматической адаптации ограничен. Автоматический произвольный dependency closure не обещан.|
|013 Переносимый пакет|**Веб:** download→paste strict reimport, perf05/context/source restored, tampering refused; cert не принимается автоматически|**Веб:** exact goal download; [11837Bpackage→paste→explicit original/bridge Verify228](../implementations/exchange/docs/evidence/portable-package-browser/README.md); edit clears readiness|**Веб:** настоящий ZIP; lake build+export побайтно совпал с perf05 goal|C public revision остановлена auto-review. Сохранённые source hashes не доказывают source→goal; конкретная C native reproduction описана отдельно. File picker и чистая установка всех зависимостей не заявлены.|
|014 Кошелёк/кабинет|**Веб:** local role/logout/private clear; injected handlers К/Т|**Веб:** local roles/SIWE/public reload; injected lifecycle К/Т|**Веб:** connect/logout/две роли, private reset; lifecycle К/Т|Реальное extension accountsChanged/chainChanged во время async import/sign-in, rejected signature. Не возвращать старое утверждение «обработчиков нет».|
|015 Каталог/описания/блоки|**Веб:** source/profile/пулы, social и сделки, события79–82|**Веб:** оба рынка, exact goal download, receipt212 с payout|**Веб:** оба рынка, Blocks, package context|Большая пагинация и диапазон block explorer; supplementary source/description не выдаются за доказанную семантику.|
|016 Обсуждения ончейн|**Веб:**56–73 — reply/edit/history/votes/ветки/tombstone|**Веб:**159–173 — ECP thread/votes/tombstone, export равен RPC|**Веб:**129–147 — EAS ветки/версии/votes/hide+restore/rebuild|Большие страницы/лимиты — К/Т, не массовый browser dataset. Запрет чужой правки/самоголосования проверяет контракт; actual direct bypass не требуется повторять вручную.|
|017 Генерация сайтом|**Отложена пользователем**; native check/import остаются|**Отложена пользователем**|**Отложена пользователем**|Ни remote CI dispatch, ни оплата proving-сервиса не являются условием текущей приёмки. История/API/CLI сохранены для будущего.|
|018 Governance через сайт|**Веб:** schedule29/advance30/execute31; точный state gating|**Веб:** propose/vote/queue/execute perf05|**Веб:** membership/profile lifecycle, preflight основного вызова|Отрицательные права, stale nonce/quorum/ETA, понятный early execute и восстановление предложения. К/Т не расширять до заявления полного browser matrix.|
|019 Вывести LP|**Веб:** полный TRUE45/46, частичный OPEN82:100→90shares|**Веб:** TRUE191/195, NO216 с последующим redeem|**Веб:** full114/166; partial181; nested185/188|A оставшиеся90LP и NO конвертация после разрешения; B fee-LP выход; recovery второй стадии конвертации и отказов. Original FPMM не имеет funding min-out — UI это явно показывает.|
|020 Полные наборы|**Веб:** split80/merge81, точная сохранность T/YES/NO, insufficient merge disabled|**Веб:** split169/merge174 и NO split204|**Веб:** split93/merge96 и NO150/nested173|Неравные стороны/не то условие и нулевые суммы — К/Т; дополнительные browser-negative ветки не названы пройденными.|
|021 Профиль и личный блог|**Веб:** профиль/версия, blog/edit/reply/vote/public reload|**Веб:** profile, blog/edit/replies/public URL/rebuild export|**Веб:** оба profile/edit, blog/history/reply/vote/hide/reload|B отдельная browser profile revision ещё не подтверждена; большие истории/chunks — К/Т. Все полные исторические bytes остаются onchain.|
|022 Governance как выгодополучатель|**Частично, веб110:** DAO20, настоящая protocol fee0.004T→claim0.0008T в Timelock, Safe2/2 consent→DAO15; [журнал](../implementations/agora/TREASURY-BENEFICIARY.md)|**UI/SDK реализованы:** Governor→Timelock treasury, Warehouse LP; живой цикл идёт, итоговый evidence ещё собирается|**Частично, веб231:** DAO20, реальные T/NO creator fees→Warehouse→Timelock; membership Governor consent→DAO15; [журнал](../implementations/vault/TREASURY.md)|Внутренняя выплата не отправлена: конкретное Vault proposal отклонено auto-review; A/B также останавливаются перед внутренним payment proposal до разрешения. Подготовка/eth_call не закрывают успешный платёж.|
|023 Управляемые эмиссия/стимулы/комиссии|**Требуется:** genesis-only T, исследуется FPMM учёт|**Требуется:** genesis-only T, ограничения original V2 изучаются|**Требуется:** genesis-only T, hooks/fee permissions требуют проверки|[Новое требование](governed-monetary-policy.md): новая версия T/миграция, реальные budget/reward/fee browser-проходы ещё не выполнены. Не менять существующие collateral/receipt задним числом.|

### Extras, независимость и оставшиеся действия

| Область | Установлено сейчас | Следующий проверяемый результат |
| --- | --- | --- |
|A Research shelf / Lean notebook|[Веб](../implementations/agora/evidence/final-scope-audit/README.md): save/edit/reopen/remove/re-add, notes restore, новый child revision, JSON export с проверкой exact hashes/parent, второй кошелёк пустой|Основные обратимые extras пройдены; notebook restore не является Lean/zk check. Приватные тексты не публикуются в evidence.|
|B full-set arbitrage / execution journal|**[Arbitrage Веб242](../implementations/exchange/docs/evidence/arbitrage-browser/README.md):** реальные LP и10T buy создали возможность; Carol исполнил10T full set, обе Swap legs и точный gross profit2.148402661908158473T подтверждены. Negative quotes/min3T disabled. **Journal:** Bob [219](evidence/exchange-journal-browser/README.md), Carol242 — actual CSV равен двум Swap events|Оба основных extras пройдены. Gas в test ETH отдельно от T profit; CSV не полная P&L. Дубль casing исправлен: identity dedup до historical reads, browser242 —18 уникальных строк вместо24, суммы сохранены; [RPC evidence](../implementations/exchange/docs/evidence/activity-identity/after.json).|
|C LP scenarios / governance preflight|[Веб scenario177](evidence/vault-false-browser-2026-09-10.md): точный wallet/inventory, смена адреса очищает; join/partial exit179/181. [Веб preflight190](../implementations/vault/evidence/deadline-browser/README.md): EOA refused `Target has no contract bytecode`, noTX|Основной preflight membership payload проверен ранее; future permissions/ETA не гарантированы симуляцией. LP scenario не обещает будущую прибыль.|
|Свежий независимый запуск A|[Реальная intended-source копия](../implementations/agora/FRESH-START.md), offset19545/14171/15171, original verifier/core/SSTORE2, RPC/API и собственный browser PASS, cleanup|Повторная загрузка npm зависимостей с нуля не измерена: использовался существующий locked node_modules. Perf05 допускается обычным governance, не bootstrap bypass.|
|Свежий независимый запуск B|[499 source files, реальный compile/deploy](../implementations/exchange/docs/PACKAGE-START-AUDIT.md), offset19546/14172/15172, original verifier/ECP, HTTP/RPC/SDK PASS, cleanup|Этот fresh-copy pass не назывался browser interaction или свежим npm install. Нужен отдельный browser пустого стенда, если завершать именно browser-часть fresh acceptance.|
|Свежий независимый запуск C|[Actual fresh runtime + browser](../implementations/vault/evidence/fresh-start/README.md): новый instance, block33, RPC/API/web/EAS/original verifier; main188 сохранён и temp ports закрыты|QA acknowledgment timeout40s/exit1 сохранён отдельно от runtime success; clean npm/toolchain download не повторён. После derived-review UI новые9tests/build PASS.|

Практический порядок остатка: завершить Exchange US022 income/consent и Vault operator workflow; внутренние выплаты после конкретного разрешения; затем оставшиеся применимые governance/allocation и wallet recovery ветки по их реальным пробелам. Deadline B уже завершён до228, A kind1 до90, Vault продолжение ожидает отдельного разрешения. После каждого шага нужны конкретные UI action, actor, tx/block/events/balances либо read-only artifact/rebuild evidence. Не повторять успешно выполненные деньги ради отчёта и не переименовывать test verifier в настоящий Lean proof.

**Остановленные действия остаются отдельными:** Agora NO settlement; Agora matching-outcome derived registration; Vault публикация выбранного source package; новый expired deadline child и resolution parent189/его детей в Vault. Пользователю переданы причины автоматической проверки разрешений, ответа на продолжение на момент среза нет. Исходные причины и параметры сохранены в [A-NO](../implementations/agora/evidence/no-cycle/README.md), [A-derived draft](../implementations/agora/evidence/claims-derived/blocked-action.json), [C-publication](evidence/vault-package-browser-2026-09-10.md). Не повторять эти действия через API/SDK как обход; продолжать независимые части. Наличие реального сертификата не заменяет разрешение на остановленную отправку.

### Граница этого обновления

Матрица объединяет сохранённые ручные browser/RPC evidence и отдельно отмеченные проверки кода/тестов. Новое proving в этих проходах не запускалось. [Постановка22US](user-scenarios.md) остаётся целиком; отложен только явно отложенный пользователем website-proving. Пины математических профилей v3/perf05 различны, genuine smoke сохраняется; инфраструктурные/mock тесты учитываются только в своих границах. Исторические «20US», «социальный слой ещё офчейн», «generic только две цели», «нет настоящего сертификата» и «remote US017 обязателен» ниже **не действуют как текущие выводы**.

---

<details>
<summary>Исторический аудит и ранние дополнения — superseded, сохранены для трассировки</summary>

> Весь блок ниже относится к прежним срезам. Его «следующие шаги», недостающие функции и описания текущего состояния не являются действующим планом. Актуальные статусы — в матрице выше; старые evidence и ошибки не удалены.

# Исторический аудит

## Актуальное дополнение, 10 сентября 2026

Exchange прошёл общий внешний bundle → регистрация → NO LP/trading → настоящее опровержение → выплаты в блоках202–218; в браузере раскрыты исход2 и CTF payout[0,1]. [Подробный журнал](evidence/exchange-false-generic-browser-2026-09-10.md). Generic import теперь реализован во всех трёх; конкретный browser pass общего формата пока подтверждён Exchange. Аудит чистого старта обнаружил ignored proof/deployment descriptors и отдельные orchestration gaps: исправляются и требуют фактического изолированного fresh-copy запуска, не только текущих работающих процессов.

Исторический аудит ниже сохранён для трассировки. Новые авторитетные browser результаты: Exchange TRUE-цикл завершён, включая выдачу protocol LP обоим получателям в блоках 200/201; Vault FALSE-цикл завершён в блоках 148–169 вместе с производным ResolvedAs(False) → True. См. [Exchange](evidence/exchange-true-fees-browser-2026-09-10.md) и [Vault](evidence/vault-false-browser-2026-09-10.md). Полученные Exchange fee LP ещё не превращены в T. Agora FALSE resolution остаётся неподтверждённым после прежнего auto-review отказа.

Полнотекстовый onchain social реализован в трёх приложениях; свежие browser/RPC результаты находятся в [onchain-social.md](onchain-social.md) и app evidence. Это заменяет старые offchain social passes. Generic external bundle import реализован и проверен в Agora/Vault, Exchange дорабатывается; настоящий сертификат совершенно новой цели этим не заявляется.

В FALSE-проходе Vault найдены и исправлены два UX-дефекта: старый LP scenario сохранялся после смены кошелька; преждевременное разрешение производного показывало unknown custom error. Повторный browser проход171/177 подтвердил понятное ожидание родителя и очистку/пересчёт для нового кошелька. Дополнительный взнос, частичный выход и вложенные производные пройдены179–188; подробности в Vault evidence выше. Дедлайны, остальные ветки и переносимость пакетов остаются самостоятельными требованиями.

**Самое позднее уточнение пользователя 2026-09-10:** социальный слой и личные блоги должны быть полностью ончейн. Поэтому прежние social passes в этом аудите больше не закрывают целевую реализацию; нужен переход по [onchain-social.md](onchain-social.md) и новые receipts/rebuild checks для всех трёх вариантов. Торговые и доказательные требования сохраняются.

**Более позднее решение пользователя 2026-09-10:** убрать генерацию/заказ новых сертификатов из сайтов текущей версии, пользователи подготавливают сертификаты сами. Поэтому перечисленные ниже gaps remote dispatch/встроенного prover US-017 теперь **отложены и не блокируют текущую приёмку**. Это исторический аудит до изменения объёма: его описание remote как главного blocker больше не актуально. Обязательны готовый внешний сертификат → точная проверка → регистрация/резолв, остальные сценарии, социальные функции и extras. Ограничение импортера двумя fixture goals не следует автоматически считать поддержкой произвольного внешнего сертификата; совместимость импорта и underlying generic onchain bridge проверяются отдельно.

Новые результаты: Exchange вручную допустил perf05 Governor/Timelock в блоках123–143; true-market lifecycle ещё не завершён. Vault прошёл социальные действия с двумя кошельками ([evidence](evidence/vault-social-2026-09-10.json)); дефект reply-order исправлен в коде, ручной повтор после API restart ещё нужен. Agora NO-цикл дошёл до verified CI6 и блока54, финальный резолв остановлен автоматической проверкой разрешений; не считается пройденным.

Срез: 10 сентября 2026 года. Это bounded read-only аудит текущего кода и сохранённых результатов, без нового запуска тестов, браузера, prover или транзакций. Он сохраняет весь объём [20 сценариев](user-scenarios.md), [социальных функций](profiles-and-discussions.md) и двух авторских возможностей каждой реализации. Прохождение одного настоящего рынка не сокращает этот объём.

Главный подтверждённый результат: **Vault и Agora прошли через браузер настоящий perf05 true-цикл**, включая original RISC Zero verification, регистрацию, ликвидность, buy/sell, resolution и redemption. Vault также выплатил protocol income обоим адресам; Agora выплатил LP-комиссию отдельному LP. **Вся задача ещё не завершена.** В частности, сайты пока не отправляют пользовательские задания в удалённый GitHub proving; импорт готового сертификата реализован отдельно. Получены настоящие сертификаты обеих сторон perf05, но полноценный NO-рынок ещё не записан как browser pass во всех трёх приложениях.

## Как читать статус

- **Веб** — конкретное действие действительно выполнено координатором в браузере; для денежных действий есть receipt и исторические балансы. Указанные блоки принадлежат только своей цепи.
- **Тест/API** — сохранённый автоматический или программный результат. Экономические suites используют настоящие AMM/CTF, но отдельный test verifier; они не доказывают настоящий Lean → zk. Нативный Lean check также не является zk.
- **Код** — найден связанный web/SDK/API/contract путь; успешный пользовательский проход не заявлен.
- **Пробел** — отсутствует часть реализации или проверки. В таблицах явно различаются «нужно реализовать» и «нужно пройти».

Не вводится общий зелёный статус US: один US часто содержит основной путь, ветку отказа и восстановление. Тест отказа может закрывать контрактный инвариант, но не подтверждает, что сайт понятно показывает этот отказ. Наличие кнопки не подтверждает её работу.

Авторитетный актуальный журнал — [manual-validation.md](manual-validation.md). Исторические фразы «сертификата ещё нет», «рынков 0» в старых app audit/README/VALIDATION относятся к моменту их записи; они не отменяют более поздние receipts. И наоборот, новые receipts не закрывают автоматически остальные строки старого аудита. [scenario-coverage.md](scenario-coverage.md) — карта требований, не результат исполнения.

## Опорные доказательства и границы

| Обозначение | Что реально зафиксировано | Что этим не доказано |
|---|---|---|
| **A-web** | Agora, chain31371: Safe schedule29, пустой time-block30, execute31; statement32, pool33, curator funding35, второй LP37, buy39/sell41, genuine resolution42, trader redeem43, LP fee44/exit45/redeem46. [Журнал](manual-validation.md#agora-завершённый-допуск-perf05-и-настоящий-рыночный-цикл), [raw evidence](../implementations/agora/.local/manual-perf05-market.json). | NO-рынок, protocol-beneficiary release, все deadline branches, обсуждения и полный пакетный pipeline. JSON paste сработал; file upload в этом проходе был заблокирован разрешением Chrome-инструмента. |
| **B-web** | Exchange, chain31372: кошелёк, профиль/SIWE, пустой каталог, Palomar listing и fixture; v3 governance выполнен скриптом, результат112 раскрыт в браузере; согласия выгодополучателей113–119 выполнены вручную. [App journal](../implementations/exchange/docs/VERIFICATION.md), [общий журнал](manual-validation.md), [allocation receipts](../implementations/exchange/data/manual-validation/beneficiary-epochs.json). | В этом срезе координатор отдельно проходит полный true-цикл Exchange; его будущий успех здесь не предвосхищается. Script migration и просмотр результата не названы ручным полным голосованием. |
| **C-web** | Vault, chain31373, новая persistent instance `554c825d-6813-43bf-9bf0-6ede06acff4d`: membership36–62, perf05 governance64–90, statement91, split93/merge96, pool97/init102, buy105/sell108, derivative109, genuine resolution110, trader redeem112, LP exit114/redeem117, derivative False118, protocol fees/claims119–125. [Журнал](manual-validation.md), [market receipts](../implementations/vault/.state/manual-perf05-market.json), [membership receipts](../implementations/vault/.state/manual-membership-governance.json). | Второй LP/join, NO-базовый рынок, deadline boundaries, публикация пакета, обсуждения и все дополнительные функции. Это история новой Anvil-сети, не восстановление потерянной volatile Hardhat-цепи. |
| **CI-real** | Четыре genuine perf05 artifacts: [true registration](evidence/ci3-perf05-registration/verified.json), [true proof](evidence/ci4-perf05-true-proof/verified.json), [false registration](evidence/ci5-perf05-false-registration/verified.json), [false refutation](evidence/ci6-perf05-false-refutation/verified.json). Original EVM принимает их и отвергает изменённые claims; копии есть внутри приложений. См. [CI validation](ci-proof-validation.md). | Это не автоматически выполненные рынки, не v3/Nat.add_comm certificates и не сервис произвольных пользовательских задач. |
| **A-test / B-test / C-test** | Сохранённые [Agora](../implementations/agora/VALIDATION.md), [Exchange](../implementations/exchange/docs/VERIFICATION.md), [Vault](../implementations/vault/VALIDATION.md) журналы; economic suites, API/social tests, targeted UI-state tests. | Tests с `TestProofVerifier`/mock verifier не превращаются в genuine zk evidence. Read-only EVM verification отдельно от wallet transaction; UI unit tests отдельно от browser pass. |

perf05 — отдельный zero-axiom профиль `0x93cf174539debc6ddce2691f3def1e36023cf77fe1d93f1692f330150bd37e10`, image `0x296fb3bbf5eb7df9fb7115826df31c71411ee0f8153970c7618c76ab01539feb`. В smoke используются две точные цели: `∀ P : Prop, P → P` и `∀ P : Prop, P` с доказательством отрицания второй. Прежний v3/Nat профиль не переименован. Замена profile/image требует отдельного допуска governance и отдельной проверки, а не правки JSON-ярлыка.

## Agora: все 20 сценариев

Основные пути: [web App.vue](../implementations/agora/src/App.vue), [SDK](../implementations/agora/sdk/index.mjs), [server](../implementations/agora/server/index.mjs), [contracts](../implementations/agora/contracts/Agora.sol). Историческая [карта реализации](../implementations/agora/COVERAGE-AUDIT.md) сохраняет полезные детали; ниже учтён поздний A-web.

| US | Текущий путь и доказательство | Следующий шаг / реальный остаток |
|---|---|---|
| **001 Создать рынок** | Create → external certificate verify → `AgoraRegistry.register` → CTF condition → `createPool` → FPMM funding. **Веб:**32/33/35. | Пройти новый NO statement с CI5. Для произвольного Lean исходника нужна доработка remote path, а не ещё один импорт фиксированного true. В `createMarket` ещё нет явного resume после успешного register и неудачного createPool: UI повторяет составную операцию; нужен recovery по receipt/statementId. |
| **002 Внести LP** | `provideLiquidity` / FPMM `addFunding`. **Веб:** curator100T и независимый Account3 +20T37, реальные LP shares. | Пройти частичное дополнительное внесение и отказ allowance/balance. Реальный UX-пробел: до approvals не показываются рассчитанные LP shares и возвращаемый остаток YES/NO; действие есть, подробного preview нет. |
| **003 Торговать** | Quote `calcBuyAmount/calcSellAmount` → `buyWithDeadline/sellWithDeadline` с min/max. **Веб:** YES buy39/sell41. Amount parser исправлен и повторный реальный обмен прошёл. | NO buy/sell; изменение котировки, expired/min-out refusal с понятным UI, wallet rejection. Контрактные price/deadline отказы покрыты A-test, не браузером. Продажа задаёт T к получению, это надо сохранять в QA. |
| **004 Доказать P** | Import CI4 → fresh original/bridge verify → `submitProof(id,1,cert)` → CTF[1,0]. **Веб:**42, gas361456; события раскрыты. | От начала сценария «свой Solution → получить proof в приложении» остаётся US017. Проверить wrong-goal/profile/outcome и повторную отправку в UI; import stale-selection защищён targeted tests, но не отдельным browser race проходом. |
| **005 Опровергнуть** | FALSE outcome2, canonical `false-refutation`, registry CTF[0,1]; настоящий CI6 доступен. **Код + tests/real EVM**, не NO market browser pass. | CI5 → создать новый false-goal рынок → split/LP/NO trade → CI6 verify → resolution → выигравшие NO и проигравшие YES redeem, записать баланс/пayout[0,1]. Истечение срока не использовать как refutation Lean. |
| **006 К сроку** | Create kind1/3 → `registerDerived`, `derivedOutcome` использует `resolvedAt <= deadline`; False после `timestamp > deadline`. **A-test**. | Реальный производный рынок: до срока unresolved, своевременный True, поздняя запись base не переписывает False. Ровно-at-boundary лучше закрыть детерминированным RPC/contract test, отдельно обозначив это; браузер подтверждает читаемую дату/часовой пояс/основание. Detail пока показывает числовой kind без полной формулы. |
| **007 Сторона резолва** | kind2 `ResolvedAs`, kind3 `ResolvedAsBy`, kind4 governance-approved operator; `resolveDerived`. **Код/A-test**. | Веб: pending base не даёт раннего False; opposite outcome даёт False после записи, expected даёт True. Detail нужно раскрывать dependency/expected как понятное правило. Custom operator остаётся raw parameters, не универсальный schema-driven editor. |
| **008 Погасить** | Original CTF `redeemPositions(...,[1,2])`. **Веб:**43 trader,46 LP; YES/NO сожжены, фактический T получен. | NO winner branch, повторный redeem, отсутствие AMM liquidity, понятная предварительная сумма T. Текущий UI не даёт отдельного количественного redemption preview, хотя действие и балансы работают. |
| **009 Расширить протокол** | `createGovernance/sign/schedule/execute` → Safe2/2 → OZ Timelock → immutable profile/operator. **Веб:** profile schedule29/execute31; две подписи ранее сохранены. | Веб: добавить другой operator и создать его statement; отказ nonowner/одной подписи, неверного manifest/profile. A-test уже проверяет настоящий Safe/operator. Универсальный arbitrary-call composer и специальные SDK lifecycle helpers ограничены; generic SDK read/write есть. Это не governance, назначающий математическую истину. |
| **010 Доли** | `proposeAllocation/consent/applyAllocation`, computed losers и immutable PaymentSplitter epochs. **Веб:**23–27, только Curator теряет, consent→revoke→consent, другой адрес apply. | Ветка нескольких одновременно теряющих, удалённый адрес→0, stale proposal, начисленная старая epoch после смены. A-test покрывает инварианты; полноценный browser двух loser's veto ещё не записан. |
| **011 Доход** | FPMM переводит protocol часть сразу в текущий PaymentSplitter; Revenue → `claimEpoch` → `PaymentSplitter.release(T,address)`. **Веб/RPC:**0.048163265306122448T начислено epoch1; LP claim44 — отдельная комиссия LP. | **Ещё не получен protocol income выгодополучателями в браузере.** На текущем Split выплатить обоим адресам, проверить repeat/old epoch и conservation. Нельзя зачесть LP fee44 как beneficiary claim. |
| **012 Внешняя формализация** | [imports.mjs](../implementations/agora/server/imports.mjs): live recent Palomar + exact GitHub commit files; Ix starter. **Веб:** три Ix drafts, первый открыт в workbench. **Native:** три wrappers проверены отдельно. | Выбор нескольких declarations/version и импорт→совместимый environment→certificate→resolution не завершены. Код не устанавливает closure зависимостей и не адаптирует любой Challenge к `Oncm.goal`. Palomar/репозиторий дают материалы, не разрешение рынка. |
| **013 Переносимый пакет** | `/package/:id`, SDK `package`, `exportPackage/importPackage`: JSON source/files/hashes/provenance и только свои completed jobs. Privacy tests пройдены. | **Реализация частична:** `importPackage/applyPackage` не проверяют advertised `fileHashes`; нет независимого Lake/Comparator rebuild и команды полной проверки exported package. Пройти export/import истинного рынка и воспроизводимую внешнюю проверку после добавления недостающего tooling. Не публиковать private jobs автоматически. |
| **014 Кошелёк/кабинет** | SIWE, `signIn/signOut`, profile и actual portfolio. **Веб:** сохранено имя/bio, новая вкладка показала сохранённый профиль/адрес; разные devnet роли использованы. | Нет injected `accountsChanged/chainChanged` handlers и явной Logout кнопки в текущем App.vue. `switchRole` чистит session/shelf/notebook, но `startJob` poll не обрабатывает исчезнувший owner job (`j` может отсутствовать). Это кодовые gaps; затем проверить rejected signature, смену injected identity/network, logout/relogin. |
| **015 Просмотр** | Explore/Overview/source/CTF/FPMM data; actual Activity logs/balances. **Веб:** настоящий рынок и раскрытый block42. | Анонимное открытие populated каталога, фильтры, exact source/provenance, смена metadata без изменения goal. Нет полноценной pagination/status/full-text navigation; Activity UI ограничивает последние блоки, API `from` есть. Не объявлять source hash семантической проверкой описания. |
| **016 Обсуждение** | Discussion/profile/reply/edit/vote/Top/New, [social.mjs](../implementations/agora/server/social.mjs), SIWE authorship. **Tests**, не полный market UI pass. | Выполнить социальную матрицу ниже двумя адресами на настоящем рынке; убедиться, что source/job privacy и payout не меняются. Назначенная локальная moderation не должна называться governance-reputation. |
| **017 Lean и certificate** | Workbench/SDK `runJob` → owned [proof-jobs](../implementations/agora/server/proof-jobs.mjs) → guarded local runner; native check реально проходил. External JSON import отдельно работает. | **Главный implementation gap:** нет web→GitHub dispatch/status/artifact delivery; expensive local proving выключен. Нужен remote job lifecycle и вход нового пакета. Доступный perf05 market прямо отключает local v3 generation; это корректная несовместимость, не готовый «Generate certificate». |
| **018 Governance UI** | Safe Sign/Schedule/Execute; live Timelock state и локальный advance. **Веб:**29–31, прежний scheduled/collecting UI defect исправлен и успешный Execute повторён. | Nonowner/rejected signature/stale Safe nonce, отказ до ETA; проверка exact targets и разрешений в полном проходе. Нет UI rebuild для stale nonce. Consent выгодополучателей — отдельное право, не Safe vote. |
| **019 Вывести LP** | `removeLiquidity`/FPMM `removeFunding`, затем CTF redemption. **Веб:** Account3 fee44/full20shares exit45/redeem46; LP/YES/NO0. | Частичный exit до resolution, полный выход оставшегося curator, accurate per-token preview и recovery между выходом/погашением. Нет guided breakdown каждого вывода; возвращённый T не обязан равняться внесённому. |
| **020 Полные наборы** | Overview Split/Merge → original CTF `splitPosition/mergePositions`. **A-test** conservation; funding внутренне split не считается ручным US020. | Веб отдельные split→merge, остатки неравных сторон, insufficient side, wrong condition отказ. UI не показывает доступный лимит merge до отправки; отдельный мастер split→LP — дополнительное удобство. |

## Exchange: все 20 сценариев

Основные пути: [web main.jsx](../implementations/exchange/web/main.jsx), [SDK](../implementations/exchange/sdk/index.mjs), [SCENARIOS](../implementations/exchange/docs/SCENARIOS.md), [VERIFICATION](../implementations/exchange/docs/VERIFICATION.md), [external import](../implementations/exchange/sdk/proof-import.mjs). Полный текущий true browser pass выполняет координатор отдельно; после его receipts обновляются именно подтверждённые ячейки.

| US | Текущий путь и доказательство | Следующий шаг / реальный остаток |
|---|---|---|
| **001 Создать рынок** | `createMath` → Registry verifyGoal → original CTF + canonical ERC20 wrappers + две V2 pairs атомарно. External perf05 import делает fresh EVM check. **Код/B-test**, genuine certificates готовы. | Веб допуск perf05 обычным Governor → CI3 import/verify → create, сохранить exact statement/pair/condition IDs. Этот шаг входит в текущий проход root. Generic пользовательская цель зависит от US017. |
| **002 Внести LP** | Seer `split` → V2 Router02 `addLiquidity` T+YES/T+NO с per-asset minima; вторые LP в B-test. | Веб обе пары, независимый LP, неидеальное ratio/неиспользованный остаток, частичное добавление и approvals failure. Не называть T+YES вклад автоматическим финансированием NO pair. |
| **003 Торговать** | Original Router `getAmountsOut` и `swapExactTokensForTokens` с minOut/deadline. B-test actual fills/rejection. | Веб YES/NO buy/sell и ухудшение цены до minOut refusal; actual receipt + кошелёк. Текущий root truecycle покрывает только совершённые им направления, остальные не выводятся из них автоматически. |
| **004 Доказать P** | External CI4 verify → exact selected statement/outcome binding → registry → CTF[1,0]. Tests import/async guards и actual original EVM read-only passed. | Настоящий browser settlement и redemption — текущий проход root. От «мой Solution» до удалённого artifact нужен US017; cached imported proof не закрывает генерацию. |
| **005 Опровергнуть** | Canonical CI5/CI6 perf05 transport; false settlement B-test использует explicit test verifier. Реальный CI6 принят оригинальным EVM отдельно. | Новый false-goal рынок через браузер, NO liquidity/trade, outcome2 submit, payout[0,1], обе стороны redemption. Не смешивать native `Nat.zero_ne_one` check с outcome2 криптографическим рыночным проходом. |
| **006 К сроку** | Create derived → recorded inclusive deadline; SDK resolve. B-test early/exact/late paths. | Веб ResolvedBy с unresolved/expired и своевременной записью, читаемый timestamp; сохранить основание финальности и невозможность переписать поздним base resolution. |
| **007 Сторона** | ResolvedAs/ResolvedAsBy; пример `UnresolvedByOperator` с web encoder; generic operator bytes. B-test operator installation/use. | Веб pending/opposite/expected side, optional deadline и новый symbol после governance. Это версия проверяемого onchain предиката, не произвольное исполнение Lean-плагина. |
| **008 Погасить** | Original Seer unwrap/redeem → CTF escrow; winner1, loser0. B-test. | Веб после genuine true и false: T delta, burned ERC20/CTF positions, repeat и отсутствие необходимости AMM liquidity. |
| **009 Расширить протокол** | Delegate T → OZ Governor/Timelock → immutable profile/operator. **Скрипт:** v3 lifecycle до112; **Веб:** результат/события просмотрены. | Root проходит perf05 governance вручную. Потом operator proposal/use; wrong caller, retired registration/resolution policies. Не приписывать голосование escrowed T первоначальному владельцу: electorate здесь liquid delegated T. |
| **010 Доли** | Propose/consent/revoke/apply → AllocationController → ownerless Split epochs. **Веб:**113–119, уменьшался Alice, применял Carol. | Два одновременно уменьшающихся, removal→0, stale proposal, начисленные активы старой epoch после смены. Contract test уже проверяет отказы, browser ветки ещё не записаны. |
| **011 Доход** | V2 `feeTo` mint protocol **LP на liquidity event** → collect current epoch → PullSplit distribute → Warehouse withdraw. B-test real LP mint/old epoch payout. | Веб сначала торговля, затем liquidity event для mint; collect/distribute/withdraw LP обоим адресам, exact LP units/dust. Сам swap не обязан дать claimable. Разделить получение LP и добровольный последующий removeLiquidity. |
| **012 Внешняя формализация** | Live Palomar recent/versioned record → exact commit files/hashes. **API:** imported real snapshot; **Веб:** listing/metadata и published core fixture. | Импорт выбранной Palomar декларации из веба, точная версия/source, compatibility refusal; нет общего dependency closure/build path и гарантии всех Lean packages. |
| **013 Переносимый пакет** | Portable JSON export/import, source/files/hash/profile/provenance/registration result; proof results download. B-test/API roundtrip. | Independent downloaded package→Lake/export/goal comparison не подтверждён и не предоставлен как полный self-contained environment. Пройти пакет настоящего рынка и независимое восстановление с pinned dependencies; JSON roundtrip не считать этим результатом. |
| **014 Кошелёк/кабинет** | Devnet/injected connection, SIWE профили, portfolio. **Веб:** Alice save name/bio и real100000T; API nonce/replay/logout. | В main.jsx нет injected accountsChanged/chainChanged handlers и явного logout flow. Добавить согласованную смену signer/SIWE/privatejobs, затем browser identity/network changes + profile reload. |
| **015 Просмотр** | Catalog/specification/actual registry+pair data; block Activity и исторические balances. **Веб:** пустой catalog и block112. | Populated anonymous catalog, source/package links/goal/profile/current outcome и переходы после текущего truecycle; проверить stale index/RPC error и отображение независимых YES/NO pairs. |
| **016 Обсуждение** | [social SDK](../implementations/exchange/sdk/social.mjs), SIWE public profiles/replies/edit/vote/Top/New. **5 API tests**, profile browser pass. | Двухадресный комментарий/reply/vote lifecycle на реальном рынке, авторский профиль, Top/New/reload; API test thread не объявляется browser market discussion. |
| **017 Lean и certificate** | Owned local proof queue/guard + native checker; native true/refutation через веб. External import and ready fixtures are separate. | **Нет web→remoteCI submission/status/download.** Local expensive=false, perf05 localRunner=false. `fixtureForCertificate` допускает два bundled perf05 goal; pinned workflow case не принимает произвольный source. Нужна реализация нового удалённого package job, не новая кнопка загрузки существующего файла. |
| **018 Governance UI** | Targets/values/calldata, state/snapshot/deadline/For-Against-Abstain показаны; vote/queue/execute SDK есть. B-test lifecycle; просмотр112. | Кодовый gap в `Governance`: не показаны actual ETA/quorum/historical wallet weight/hasVoted; Execute виден для любой Queued, votes не блокируются по hasVoted. +14blocks может перепрыгнуть Active при delay1/period12. Добавить exact views и безопасный шаг блока; затем вручную pre-ETA refusal, repeated vote и полный lifecycle. Это UI gap, не обход Governor. |
| **019 Вывести LP** | Original V2 removeLiquidity → T+wrapped outcome → optional redeem; B-test after resolution, V2 swaps остаются открыты. | Веб частичный/full exit, both pair tokens, winning redemption/losing zero; сохранить minima/residual balances. Не обещать остановку V2 после payout: это сознательно другая архитектура. |
| **020 Полные наборы** | Original Seer router + CTF split/merge; B-test escrow/mismatched amounts. | Веб split→merge с точными units и недостаточной одной стороной; wrong condition и повторный redeem — контрактные отказы плюс понятный UI. |

## Vault: все 20 сценариев

Основные пути: [App.svelte](../implementations/vault/web/App.svelte), [SDK](../implementations/vault/sdk/index.mjs), [COVERAGE-AUDIT](../implementations/vault/COVERAGE-AUDIT.md), [VALIDATION](../implementations/vault/VALIDATION.md). G1/G2/G3/G6/G8 исторического аудита реализованы; G5 lifecycle review существенно закрыт. Они не должны снова называться отсутствующим кодом только из-за старой таблицы.

| US | Текущий путь и доказательство | Следующий шаг / реальный остаток |
|---|---|---|
| **001 Создать рынок** | External verify → `register` → CTF/wrappers; `PoolCoordinator.create` → original WeightedPoolFactory. **Веб:**91/97. Source publication отдельная explicit операция. | Новый false-goal рынок с CI5; проверить recover между registry/pool/init. Для нового собственного Lean нужны US017/generic packages. Публикацию source не считать выполненной фактом наличия goalHash. |
| **002 Внести LP** | `quoteLiquidity` → initialize/join exact/max per token → Permit2 + original Router. **Веб:**45T+90YES init102→63.639610306787004170BPT. | Второй независимый LP и proportional join, changed input/stale quote refusal, NO/T pool. Прежний TRANSFER_FROM_FAILED был настоящим отказом, затем исправлено количество; это не успешный тест второго LP. |
| **003 Торговать** | Original zero-origin query → saved minOut/deadline → Router exact-in. **Веб:** Account1 buy10T105/sell5YES108. | NO direction, rejected stale/min-out/expired transaction, wallet rejection. Fixed quote limits tested; new verification не должна пересчитать minOut после approvals. |
| **004 Доказать P** | CI4 load→original+bridge verify→apply selected→`submitProof`. **Веб:**110 genuine, outcome1/events/receipts. | Собственный Solution→remote proof остаётся US017. Wrong goal/profile/outcome, selection switch while async verify, repeated finalization: targeted tests есть, специальный browser negative path ещё не записан. |
| **005 Опровергнуть** | Exact false goal и canonical false-refutation import; original bridge supports outcome2. Genuine artifacts локально доступны. | Browser CI5→NO market→CI6→payout[0,1]→NO winner/YES loser redeem. Наличие CI6 и synthetic transport unit test не равно этому проходу. |
| **006 К сроку** | ResolvedBy/AsBy → registry reads resolvedAt and inclusive deadline; форма показывает timezone. C-test. | Реальные before/after deadline ветки на ещё unresolved base, timely and late resolution. Не пытаться демонстрировать их изменением timestamp уже finalised рынка; отдельные derived IDs/fixtures нужны заранее. |
| **007 Сторона** | Builtins plus governed operator/module; `resolveDerived`. **Веб:** `ResolvedAs(base,False)`109 при base unresolved, после True110 стал False118. | Same-side True, still-pending refusal, AsBy, новый module. Универсальная typed operator schema UI остаётся G4; raw SDK/bytes path существует, поэтому отсутствие autogen формы не называется отсутствием extensibility. |
| **008 Погасить** | PositionRouter unwrap → original CTF redeem → delta T user. **Веб:**112 trader11.22950819672131144T,117 LP и проигравшие NO сожжены. | NO outcome winning branch, partial amounts SDK, repeat/dust, explicit no-liquidity redemption. LP exit не обязателен владельцу обычных outcome tokens. |
| **009 Расширить протокол** | Profile/operator/member/custom proposals → real OZ Governor/Membership/Timelock. **Веб:** membership36–62 и perf0564–90. Исторический snapshot37 у принятого member остаётся0. | Новый operator ID→statement use; decline/cancel/retired profile semantics. SDK arrays/values есть; web composer одного value0 call и отсутствие historical registry diff/proposal discussion — оставшиеся удобства, не отсутствие Governor lifecycle. |
| **010 Доли** | AllocationController computed losers/base epoch → consent/revoke/apply immutable PullSplit. **Веб:**31–35, единственный decreasing Account1, эпоха2 50/50. | Несколько теряющих/removal/stale base. Old epoch сохранена по чтениям; нужно начислить/получить ненулевой old-epoch claim после переключения и показать refusal при недостающем согласии. |
| **011 Доход** | `collectAggregateFees/withdrawPoolCreatorFees` → current epoch PullSplit → Warehouse → wallet. **Веб:**119–125 оба адреса T+YES; exact historical dust accounting. | Просмотреть upstream pending stages на funded pool, old epoch payout, повторный zero claim. После account0→1 Income кнопки требовали re-click; code fix `refresh→loadClaims` есть, build прошёл, **browser retest этого fix ещё не записан**. |
| **012 Внешняя формализация** | [palomar.mjs](../implementations/vault/server/palomar.mjs) exact id/version/commit/files/SHA256; Lean Lab recent picker. API/code evidence. | Browser import/source inspection/compatibility error; G7: UI latest-version/first theorem, no multi-declaration choice и dependency install/execution. Полный внешний пакет не гарантирован текущим v3 profile. |
| **013 Переносимый пакет** | [PackageEditor](../implementations/vault/web/PackageEditor.svelte), [publications](../implementations/vault/server/publications.mjs): Challenge/Solution/Runner, toolchain, Lake config/optional lock, manifest, ZIP. Unit + authenticated API prepare/download/unzip passed. | **Часть реально реализована больше, чем evidence JSON.** Но browser public publish/export и независимый `lake build`/runner goal comparison не пройдены. Dependencies не vendored, Palomar compliance не обещана. Challenge/Solution source→onchain goal требует реальной проверки; file hashes этого не доказывают. |
| **014 Кошелёк/кабинет** | SIWE, owner profile/jobs, explicit logout и injected event cleanup. **Веб:** неизменённые profile fields сохранены и reload/deep link работает; роли0/1/3 в monetary flow. | Изменить name/bio→reload/persistence, rejected SIWE, injected account/network change, logout и old-privatejob denial через UI. Неизменённый save не выдаётся за тест редактирования произвольного текста. |
| **015 Просмотр** | Research catalog→formal content/publications/pool states; Blocks actual decoded logs. **Веб:** populated true market и block110. | Anonymous source publication и ревизия description без смены formal identity; catalog filters/deeplinks, source unavailable и RPC freshness. Публичная author-supplied постановка чётко отделена от проверенного goal/profile. |
| **016 Обсуждение** | Comment/reply/edit/profile/up/down/Top/New, [community.mjs](../implementations/vault/server/community.mjs), SIWE identity, persistent revisions. Tests. | Полный browser social matrix ниже на true market, role switch и profile link; source/privacy и governance weights не должны меняться. |
| **017 Lean и certificate** | Lean Lab native check прошёл; owner queue/status/cancel/history/download/restore существует, guarded local worker. External import real. | **Нет UI/API provider для отправки/отслеживания remoteCI.** Local expensive=false; fixed perf05 import является доставкой готового результата. Нужно generic immutable package job, explicit privacy/storage choices и артефактная проверка при возврате. |
| **018 Governance UI** | [GovernanceProposal](../implementations/vault/web/GovernanceProposal.svelte) / [SDK](../implementations/vault/sdk/governance.mjs): exact targets/calldata/values, current state, ETA, snapshot/deadline/quorum/hasVoted/historical weight; full action eth_call. **Веб:** membership+profile lifecycle. | Pending cancel/Defeated/no-votes/new-member historical rights и multicall review через SDK. Дополнительный comments/diff/composer не выдавать за уже готовую функцию. +1block helper уже есть и использован. |
| **019 Вывести LP** | Original Router proportional exit с quote/per-token minima; FinalityHook разрешает remove. **Веб:** full BPT exit114 и redeem117, BPT/YES/NO0. | Частичный exit до settlement, второй LP/full exit, stale minima; direct Router post-resolution initialize/join/swap rejection уже contract-test-only. Hook не исключает pre-resolution toxic flow и не гарантирует доход LP. |
| **020 Полные наборы** | PositionRouter→original CTF split/merge↔canonical wrappers. **Веб:**100T split93,10merge96, осталось90YES+90NO. | Insufficient/mismatched sides и wrong condition UI path; repeat burn/redemption protected contract tests. Полный набор не является BPT и не должен смешиваться с liquidity shares. |

## Обязательные профили, ответы и голоса

Это общая часть US014/016, а не одна из двух extras. Во всех трёх есть persistent offchain implementations с SIWE principal; имя не скрывает адрес. Голос комментария не даёт Governor/Membership вес и не назначает исход.

| Проверка | Agora | Exchange | Vault | Конкретный следующий ручной шаг |
|---|---|---|---|---|
| Profile displayName/bio + public address | **Веб:** отредактирован, save, открытие новой вкладкой. | **Веб:** `Alice · Number Theory` и bio сохранены; API persistence. | **Веб:** save неизменённых полей + reload; test persistence. | Vault изменить текст/reload; Exchange fresh reopen; во всех открыть чужой профиль из comment author. Не сохранять тестовые секреты. |
| Session-derived authorship / spoof rejection | service/API tests | 5 social API tests включают spoof | community/API tests | Anonymous read, unsigned write refusal, signerA не может записать profile/comment от B. Денежная транзакция для комментария не требуется. |
| Root comment + reply with same statement parent | код/tests | код/API tests | код/tests | A пишет comment; B отвечает; открыть ветку/reload. Попытка parent другого рынка должна быть отказом API; UI сохраняет правильный контекст. |
| One vote/address; +1→-1→0; no self-vote | код/tests | код/API tests | код/tests | B upvotes A, повтор не удваивает, switch down, remove; A selfvote disabled/refused. Записать score/myVote на каждом шаге, без gas. |
| Top/New deterministic ordering | social sorting tests | API sort tests | community tests | Два comment с разными score/time, переключить Top/New; равенства закрыть автоматическим tie-break тестом, не утверждать браузерную проверку одинаковых timestamp без неё. |
| Author-only edit / revision history | код/tests | код/API tests | код/tests | A edit, B refusal, показать history если UI предоставляет. Moderation — отдельная локальная политика, не математический resolver. |
| Identity switch / private-state isolation | **Кодовый gap:** injected events/logout/poll. | **Кодовый gap:** injected events/logout. | handlers/logout реализованы, targeted state tests. | После закрытия gaps сменить injected wallet/account/network; previous SIWE/jobs/private source не должны остаться как данные нового owner. |
| Persistence / disconnect | profile browser, store tests | profile + store/API tests | unchanged-profile browser, store tests | Reload app и re-auth без chain reset; сохранённые public profile/thread видны, private history только owner. Нельзя считать удаление data-dir поддерживаемым восстановлением. |

Кодовые источники: Agora [social](../implementations/agora/server/social.mjs), [tests](../implementations/agora/tests/social.test.mjs); Exchange [social SDK](../implementations/exchange/sdk/social.mjs), [API tests](../implementations/exchange/tests/social-api.mjs); Vault [community](../implementations/vault/server/community.mjs), [tests](../implementations/vault/test/community.test.mjs). Наличие этих тестов не заменяет ещё отсутствующий полный browser thread/vote pass.

## Две авторские возможности каждой реализации

| Приложение / extra | Реализация и имеющееся доказательство | Что ещё пройти |
|---|---|---|
| Agora: личная Research shelf | `saveBookmark/shelf/removeBookmark`, SIWE owner, upsert private notes; [описание](../implementations/agora/EXTRA-FEATURES.md), [research.mjs](../implementations/agora/server/research.mjs), owner/persistence tests. | Веб настоящий market→Save to shelf→notes edit→reopen→Remove; другой кошелёк не видит записи/notes. Ручного market→shelf pass ещё нет. |
| Agora: Lean notebook revisions | append-only title/source/profile/hash/basedOn; `saveRevision/notebook`, tests. **Веб:** save→edit→restore оригинала действительно пройден. | Новый revision с basedOn, export JSON и проверка exact source/hash, wallet isolation. Restore текста не является LeanAccepted или ProofReady. |
| Exchange: atomic full-set arbitrage | Original Seer split + две original V2 продажи, ownerless executor, minProfit/all-or-revert; [описание](../implementations/exchange/EXTRA-FEATURES.md). B-test реальный profitable imbalance, forced impossible-minimum revert с сохранением user/reserve balances, затем profitable execution. | Browser quote на реальных двух funded pools; при отсутствии прибыли честный отказ. Подготовка рыночного imbalance — реальные обычные trades, не fake quote. Записать обе Swap legs, gas отдельно; пока browser arb pass нет. |
| Exchange: execution journal + CSV | `executions/executionsCSV` actual V2 Swap/logs/tx sender/block; B-test four fills/three tx и absence false LP fills. | После root truecycle открыть journal, фильтры YES/NO, сравнить receipt prices/nominal input fee, export CSV. После arb две legs одной tx. Не называть это полной налоговой/P&L отчётностью. |
| Vault: LP settlement scenarios | `settlementStress`/`settlementInventory` из actual Vault balances/BPT share, floor/int mapping T/YES/NO; [описание](../implementations/vault/EXTRA-FEATURES.md), [capital tests](../implementations/vault/test/capital.test.mjs). | До выхода funded LP открыть «Сценарии True/False», записать observation block и текущий inventory, затем сравнить composition. Предыдущий LP уже вышел, для meaningful UI QA понадобится новый реальный LP/рынок. Не обещать будущую прибыль или неизменный inventory. |
| Vault: governance call preflight | Actual Timelock-address eth_call, known target decoding, rejects EOA; integration tests. **Веб:** при membership proposal coordinator предварительно проверил exact `setMember` payload. | Отдельно показать успешный preflight и конкретный revert/EOA refusal, block/target/value/calldata; убедиться, что simulation не создала tx. Governor future permissions/ETA этим не гарантируются. |

## Существенные implementation gaps и зависимости

### 1. US017: удалённое вычисление из приложения ещё не реализовано

Все три owned queues вызывают локальный `proof/runner.mjs`; все три `proof/execution-policy.local.json` содержат `allowExpensiveProving:false`. У Agora/Vault это [server/proof-jobs.mjs](../implementations/agora/server/proof-jobs.mjs) и [Vault adapter](../implementations/vault/server/proof-jobs.mjs); у Exchange [api/proof-jobs.mjs](../implementations/exchange/api/proof-jobs.mjs). Native check доступен; включать тяжёлое proving обратно для «закрытия» строки нельзя.

Работающий удалённый backend существует **на уровне coordinator CI**: [.github/workflows/lean-zk-smoke.yml](../.github/workflows/lean-zk-smoke.yml), [CI runner](../tools/lean-zk/ci/run.py). Его `workflow_dispatch` принимает только `profile: perf05|v3` и один из четырёх `case` либо `all`. Сайты не dispatch-ят workflow, не сопоставляют run с SIWE owner, не показывают remote queue/logs/cancel, не забирают возвращённый artifact автоматически. Root запускал CI отдельно, затем импортировал настоящий результат. Это полезный реально работающий этап, но не полное исходное «нажать в сайте и получить certificate».

Минимальная оставшаяся работа: provider interface для submit/status/cancel/artifacts, immutable input/package digest, owner-bound job ID и retry, server-only ограниченное полномочие доступа к CI, очередь одного heavy job, видимые timeout/cost/privacy rules. Возвращаемый сертификат должен пройти существующую exact binding + original EVM + governed bridge проверку; `evmVerified` из JSON не является доверенным флагом. Отмена/сбой должны сохранять историю и не оставлять child computation. CI secrets не отправляются в web/SDK клиент; package source нельзя автоматически публиковать из чужих private jobs.

Пользователь выбрал бесплатный GitHub CI. Это не требует дальнейшей покупки Boundless/Bonsai; backend capacity/очередь и GitHub limits остаются операционной зависимостью. Выбор GitHub сам по себе не реализует интеграцию с приложениями.

### 2. Generic Lean и fixed perf05 — разные степени покрытия

Настоящий guest проверяет Lean exports; это не текстовый mock. Но текущая доставка certificate имеет ограниченный product envelope: заранее pinned CI source/NDJSON, два known perf05 goals и их четыре artifacts. App external certificate descriptors/importers сверяют именно эти cases. Новый arbitrary theorem не становится поддержанным от того, что у него есть текст `Oncm.goal`.

v3 native runner успешно проверяет конкретные пользовательские wrappers, включая три [Ix examples](imports/ix-starter/README.md), при своих foundation/profile rules. Это подтверждает часть общего ядра, но не получение новых zk receipts/market registration этих целей. v3/Nat.add_comm и Ix ZK ещё не доведены до принятого smoke в данном срезе. Полная композиция Mathlib/внешних библиотек с pinned dependency closure остаётся [архитектурной работой](research/compositional-lean-environments.md), а не обещанной совместимостью любого Lean repository.

Чтобы закрыть general-use requirement: immutable package→целевой declaration→export closure/foundation compatibility→новая registration/proof job→actual receipt→импорт по проверенным metadata, без fixture-only lookup. При неизменном допустимом foundation новый theorem может идти под существующим profile; изменение trusted environment/image требует отдельного profile/governance. Формат и policy надо расширить явно; нельзя просто снять goal allowlist и считать любой source привязанным к certificate.

### 3. US012/013: импорт файлов не равен переносимой проверке

Все три умеют взять публичные материалы exact snapshot и сохранить provenance. Ни одна не подтверждает полный произвольный Palomar→dependencies→Comparator/Lean→zk→settlement. Agora JSON import не проверяет заявленные file hashes — конкретный недостающий validation step. Exchange JSON roundtrip сохраняет материалы, но не доказывает fresh environment reproduction. Vault имеет настоящий минимальный Lake ZIP; всё ещё нужны независимый build/check и сравнение resulting goalHash/profileId с chain context. Загрузка package не даёт ему право выполнять произвольный Lake config без sandbox/ресурсной политики.

Ручной следующий шаг для каждого: экспортировать конкретный настоящий рынок, открыть точные Challenge/Solution/toolchain/lock/metadata, сохранить hashes и проверить в отдельной доверенной среде. Нужные credentials/публичность относятся только к выбранной remote delivery; private source не публикуется по совпадению goalHash. Palomar acceptance/LLM description checks не становятся oracle.

### 4. Независимые UX gaps, не заблокированные отсутствием proof

| Приложение | Конкретный файл/операция | Значение |
|---|---|---|
| Agora | `src/App.vue createMarket` | Resume составного register/createPool/fund после частичного успеха; existing statement должен быть доступен без повторной регистрации. |
| Agora | `src/App.vue importPackage/applyPackage`, package service | Проверка schema/file hashes/goal context перед доверием imported package fields. Imported certificate отдельно проходит crypto check. |
| Agora | `src/App.vue switchRole/startJob` | Injected identity handlers/logout и безопасный poll при отсутствии job нового owner. Это статически найденная ветка, не заявленный новый browser reproduction. |
| Agora | `liquidity/redeem` UI/SDK | LP-return/withdraw/redeem preview, читаемые derived predicate и промежуточные receipts. Контрактные операции уже существуют. |
| Exchange | `web/main.jsx Governance` + governance API views | ETA/quorum/hasVoted/historical weight, персональная доступность действий, небольшой шаг mining. Governor остаётся реальной защитой; UI должен объяснять отказ до отправки. |
| Exchange | `web/main.jsx` wallet/session lifecycle | Injected account/network event handling и logout, чтобы owner-bound private формы не оставались за старым signer. |
| Vault | `App.svelte refresh/loadClaims` | Исправление disappearing claims после смены кошелька уже внесено; только повторный browser regression остаётся. |
| Vault | `PackageEditor/publications`, Palomar picker | Код publication/ZIP есть; browser publish/reproduction ещё нет. Multi-version/declaration navigation ограничена. |

Автоматические типизированные формы новых operators, proposal discussions/исторический registry diff и сложный multicall composer — явно оставшиеся удобства. Наличие raw SDK/governed module path нужно учитывать: эти ограничения нельзя смешивать с отсутствием onchain extensibility. Напротив, remote user job и независимая воспроизводимость пакета являются материальной частью исходных сценариев.

## Следующие ручные проходы в порядке зависимостей

1. **Закончить root Exchange truecycle** и добавить фактические receipts. Не сбрасывать ни одну цепь и не создавать тестовый market с mock verifier. Остальные browser строки меняются только по зафиксированным действиям.
2. **Социальный проход и extras без вычислений:** на существующих рынках два кошелька, profile/reply/vote/edit/Top-New, Agora shelf/notebook, Exchange journal/CSV; Vault preflight. LP scenario/arb требуют реальной funded позиции/двух pools и при необходимости отдельных обычных transactions.
3. **Protocol income:** Agora beneficiary PaymentSplitter release; Exchange actual V2 protocol LP mint→collect/distribute/withdraw; Vault account-switch Income retest и old-epoch ветка. Суммы в исходных активах, LP fee не подменяет protocol fee, dust сохраняется явно.
4. **Настоящий NO-cycle во всех трёх:** CI5+CI6 уже снимают криптографический artifact gate для двух fixed perf05 goals. Создать false-goal market, LP/NO trades, verify/submission, payout[0,1], redemption/LP exit, raw blocks/balances. Никакого нового prover для этих готовых artifacts не требуется.
5. **Производные и failure paths:** пока base unresolved, создать ResolvedBy/ResolvedAs/ResolvedAsBy, пройти pending/timely/expired/opposite. Граничное равенство timestamp и некоторые malicious binding случаи можно фиксировать deterministic tests отдельно от браузера. LP partial/additional, refusal limits, winner/loser/repeat, несколько losing beneficiaries, governed operator и права кошелька также отдельные проверки.
6. **US012/013 и general US017:** сначала реализовать gaps пакета/remote job, затем новый компактный публичный Lean theorem (не переименованный perf05 fixture) → подготовка/экспорт→реальное remote proving→EVM→market lifecycle. Первый такой результат и source/profile должны сохраняться переносимо. Более крупные библиотеки/FLT остаются отдельной подтверждаемой capacity/compatibility зависимостью.

Для каждого нового прохода сохранять actor, explicit UI action, statement/profile/condition/pool IDs, block/hash/timestamp, tx/receipt/decoded events и balances до/после, либо owner-bound job/artifact/hash для офчейн-действия. Уже выполненные browser денежные действия не повторять ради красивого списка; используемые старые traces сохранять. Эта таблица является продолжением полной задачи, а не основанием завершить её после базового smoke.

</details>
