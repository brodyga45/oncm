# US012/013 and independent startup audit — 2026-09-10

| Path | Actual implementation and evidence | Boundary / next manual step |
| --- | --- | --- |
| US012 Palomar → draft | API validates registry ID/version, fetches a full pinned repository commit and declared files, stores file SHA-256 and the full registry record. The first declared theorem is initially selected. Earlier seven API checks included a real Palomar import. | No new live Palomar request or browser import was run in this audit. The import does not gather the entire dependency closure, compile the source or establish source→canonical-goal correspondence. Packages must fit the documented local size limits. |
| US013 generic saved package → draft | Actual API serializer exercised in an isolated temporary filesystem; exact source, goal export, profile and imported files roundtrip. File/source hashes are checked when advertised; generic export/certificate/profile binding is always checked. A goal-only package retains no invented source. | Reimport requires explicit original-verifier/bridge verification. Known genuine perf05 artifacts exercise the cryptographic path; no third-theorem proof was generated. A browser downloaded-package reimport remains a distinct manual check. |
| Certificate readiness | `preparePortablePackage` clears readiness for both generic and legacy packages; artifact JSON is placed in the verification field. `adoptPackage` applies this to file upload and Research drafts. `retainPackageMetadata` keeps only exact source/goal/profile-matching imported provenance through verification. | Raw ABI-only legacy packages require the corresponding external artifact. Neither uploaded `verified` flags nor a claimed semantic relationship are trusted. |
| Public/private boundary | Create no longer attaches `job?.result` automatically. Explicitly uploaded public package fields can be preserved; private job history remains separate. | This prevents stale private check/job output from being attached to a new unrelated public market package. Source explicitly supplied during Create is public. |
| Fresh source startup | Tracked bootstrap contains original bridge bytecode plus exact SHA-256/image/profile/manifest pins. Startup restores only a missing runtime descriptor. Missing saved contract code fails closed; cleanup stops only owned processes. ECP compiles/deploys before web/API start. | Native Lean/prover setup remains optional and separate for external-certificate use. Fresh default protocol admits v3; perf05 requires normal governance installation. |
| Actual isolated startup |499 public source files/5,952,574B copied without data, compiled artifacts, generated web files, proof descriptor or native binaries. Existing locked node_modules was symlinked. Actual Solidity compilation, real bridge/ECP deployment and HTTP/API/RPC reads passed on19546/14172/15172. Fresh chain reached38, with no market or proof generation. | This is not a fresh npm installation or a fresh browser interaction. Exact source inventory is public. Later package metadata-retention hardening was covered by final unit tests/production build; it did not change startup mechanics. |

The final targeted suite passed **35/35**, including the original19 generic certificate tests plus package/startup/bootstrap/endpoint cases. VM startup tests execute the actual startup module with process/RPC doubles; the separate fresh-copy smoke deploys original contracts for real. A successful Vite transformation in that smoke was followed by a separate production build of current source, **2.23s**, peak446,961,344B under its guard.

Actual fresh-copy whole-process-tree measurement: **70.934s**, peak617,154,584B, exit0, no cleanup errors. All isolated ports were checked closed. The active Exchange chain9546 was never contacted by that smoke. See [public standalone evidence](evidence/README.md) and [VALIDATION.md](../VALIDATION.md).

Next browser step for US013: download the False market's portable package, open Create in a new draft, import it, confirm the correct profile/source/export and disabled Create, verify explicitly, then cancel without attempting to duplicate the existing market. Repeat with an edited advertised source/file hash and verify rejection. This is a proposed manual check, not a recorded pass.
