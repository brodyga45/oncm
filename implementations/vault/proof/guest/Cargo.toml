[package]
name = "oncm-lean-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "=3.0.6", default-features = false, features = ["std"] }
oncm-lean-checker = { path = "../checker" }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
