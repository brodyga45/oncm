import test from 'node:test';import assert from 'node:assert/strict';import {parseLocalDeadline,derivedReview,reviewedDerivedArguments} from '../src/derived-review.mjs';
const markets=[{id:'0xbase',metadata:{title:'Existing base'}}];
test('strict calendar rejects normalization and preserves exact local UTC Unix review',()=>{for(const s of ['2027-02-29 12:00','2026-13-01 00:00','2030-01-01','2030-01-01 25:00'])assert.equal(parseLocalDeadline(s).valid,false);const d=parseLocalDeadline('2030-01-01 00:00:00');assert.equal(d.valid,true);assert.equal(Date.parse(d.utc)/1000,d.unix);assert.match(d.local,/UTC[+-]/);});
test('Agora reviewed args normalize ResolvedBy expected0 and preserve exact deadline bigint',()=>{const r=derivedReview({kind:1,dependencyId:'0xbase',deadlineInput:'2030-01-01 00:00',targetOutcome:2,markets});assert.deepEqual(reviewedDerivedArguments(r),[1,'0xbase',0,BigInt(r.deadline.unix)]);assert.equal(r.targetLabel,'Either recorded outcome');});
test('outcome-only and outcome-with-deadline have distinct canonical arguments',()=>{const r=derivedReview({kind:2,dependencyId:'0xbase',deadlineInput:'bad',targetOutcome:1,markets});assert.deepEqual(reviewedDerivedArguments(r),[2,'0xbase',1,0n]);const t=derivedReview({kind:3,dependencyId:'0xbase',deadlineInput:'2020-01-01 00:00',targetOutcome:2,markets});assert.equal(t.args[2],2);assert.ok(t.args[3]>0n);assert.throws(()=>reviewedDerivedArguments(derivedReview({kind:1,dependencyId:'missing',markets})));});
